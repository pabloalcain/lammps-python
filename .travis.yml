language: python
python:
  - "2.7"

# command to install dependencies
install: "pip install -r requirements.txt"
# command to run nosetests
script: nosetests test/test_analysis.py:TestMSTE --with-coverage --rednose --cover-package=analysis -v
