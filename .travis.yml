language: python
python:
  - "2.7"

# command to install dependencies
install: "pip install -r requirements.txt && python setup.py install"
# command to run nosetests
script: cd analysis && make && cd ../test && nosetests . --with-coverage --rednose --cover-package=analysis,refactor -v
